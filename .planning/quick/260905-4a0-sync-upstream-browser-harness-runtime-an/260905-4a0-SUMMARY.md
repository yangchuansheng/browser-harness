---
quick_id: 260905-4a0
subsystem: upstream-sync
tags: [rust, daemon, cdp, mcp, migration]
status: complete
upstream_from: 0c9b95ff6740556dd71d28f9422a953d203358af
upstream_to: 10b2086c29f0696a6712956d2914e03012f5ebd0
commit: b3b67bb
commits: [2231901, d36db6d, b3b67bb]
tasks: 3
coverage:
  upstream_commits: 46
  domain_mapping: 111/111
---

# Quick Task 260905-4a0 Summary

Rust lifecycle parity through upstream 0.1.13: fingerprinted single-flight approval ownership, marker controls, transient detach precedence, Brave Origin discovery, MCP/install evidence, and audited operator guidance.

## Task Commit

- `2231901` `feat(260905-4a0): sync upstream browser harness 0.1.13`
- `d36db6d` `fix(260905-4a0): protect pending daemon successors`
- `b3b67bb` `fix(260905-4a0): separate daemon stop ownership paths`

## Delivered

- Added per-name advisory locking plus atomic `{pid,started}` daemon records; pending approval callers join one live generation and cancellation snapshots generation plus socket state before lock acquisition. The in-lock branch preserves pending daemons that become ready and successor generations; an already-ready matching generation receives graceful shutdown and exact-generation escalation. Reused-PID and legacy ownership stay protected.
- Added `BH_TAB_MARKER` disable spellings, target-session detach precedence, Brave Origin profile/launch selection, and workspace version `0.1.13`.
- Updated `SKILL.md` and the migration audit with a chronological 46-row disposition table. Python recorder/build changes are explicitly classified as upstream-only or structurally inapplicable.

## Verification

- `cargo fmt --manifest-path rust/Cargo.toml --all -- --check` — passed.
- `cargo check --manifest-path rust/Cargo.toml --workspace` — passed.
- `env -u CFLAGS -u CC cargo test --manifest-path rust/Cargo.toml --workspace` — passed across workspace crates and binaries with 0 failures; `bh-daemon` reports 24 tests.
- Focused `bh-daemon` regression tests — passed: exact pending generation cancellation, successor preservation, ready-socket preservation, legacy ownership preservation, and target-JS evaluation/detach precedence.
- `env -u CFLAGS -u CC cargo build --manifest-path rust/Cargo.toml --workspace` — passed.
- `bhrun summary` — `execution_model=PersistentRunner`, `operations=42`.
- `browser-harness --help` — passed; lists `mac-approve`, daemon lifecycle, and runner commands.
- MCP stdio `initialize` and `tools/list` — passed; `browser-harness` `0.1.13`, 18 tools.
- Audit table check — `audit_rows=46`; union domain transform — `domain_mapping=111/111`.
- `git diff --check` — passed.
- `scripts/scan_sensitive.sh` reached unsupported macOS Bash 3.2 `mapfile`; exact 12-pattern zsh/rg fallback scanned 289 tracked/unignored files with `matches=0`, `new_matches=0`.

## Deviations from Plan

- [Rule 1 - lifecycle safety] Changed the legacy plain-PID cleanup test to preserve unverifiable ownership, matching the final upstream PID-reuse safety contract.
- [Rule 3 - macOS scan compatibility] Used the documented exact-pattern zsh/rg fallback after Bash 3.2 rejected `mapfile`.
- [Rule 1 - lifecycle safety] Corrected the cancellation race found in verification: the intended pending generation is snapshotted before acquiring the lifecycle lock, so a successor published during lock wait cannot become the cancellation target.
- [Rule 1 - lifecycle safety] Separated pending cancellation from existing ready-daemon shutdown: the in-lock socket snapshot gates pending SIGTERM, and a changed ready generation receives no shutdown request.

## Remaining Handoff

- This executor intentionally left this quick PLAN/SUMMARY and `.planning/STATE.md` uncommitted for the orchestrator. No push was performed.
