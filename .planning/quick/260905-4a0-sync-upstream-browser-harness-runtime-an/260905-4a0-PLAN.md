---
status: planned
created: 2026-09-05
phase: quick-upstream-sync
plan: 260905-4a0
quick_id: 260905-4a0
slug: sync-upstream-browser-harness-runtime-an
type: execute
wave: 1
depends_on: []
autonomous: true
requirements:
  - SYNC-01
  - SYNC-02
  - SYNC-03
  - SYNC-04
files_modified:
  - rust/crates/bh-daemon/src/lib.rs
  - rust/crates/bh-discovery/src/lib.rs
  - rust/bins/bhd/src/main.rs
  - rust/bins/bhctl/src/main.rs
  - rust/bins/browser-harness-cli/src/main.rs
  - rust/Cargo.toml
  - rust/Cargo.lock
  - SKILL.md
  - .planning/migration/upstream-sync-2026-04-21.md
  - .planning/STATE.md
  - .planning/quick/260905-4a0-sync-upstream-browser-harness-runtime-an/SUMMARY.md
estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Local Chrome startup has one pending approval connection per daemon name, and concurrent callers join that exact generation."
    - "Explicit cancellation targets only the fingerprinted pending generation while reused or unverifiable PIDs and successor generations remain untouched."
    - "BH_TAB_MARKER opt-out values suppress every title-marking path while default behavior stays enabled."
    - "Target-scoped JavaScript always attempts transient-session cleanup, preserves evaluation failures, ignores only Chrome's already-dropped-session errors, and surfaces other detach failures after successful evaluation."
    - "macOS discovery and relaunch select Brave Origin for its own profile directory."
    - "The Rust workspace reports version 0.1.13, keeps the installed MCP binary available, and documents the current default-daemon browser workflow."
    - "The migration audit accounts for every one of the 46 upstream commits and records applicable, already-satisfied, and structurally inapplicable decisions with verification evidence."
    - "The committed migration is pushed through a fast-forward-safe main update and local HEAD exactly matches origin/main afterward."
  artifacts:
    - path: "rust/crates/bh-daemon/src/lib.rs"
      provides: "Shared pending-generation identity, atomic PID publication, spawn serialization, exact cancellation, marker gating, and transient detach semantics"
    - path: "rust/bins/bhctl/src/main.rs"
      provides: "Single-flight ensure/restart orchestration and Brave Origin launch selection"
    - path: "rust/crates/bh-discovery/src/lib.rs"
      provides: "Brave Origin macOS profile discovery"
    - path: "rust/Cargo.toml"
      provides: "Workspace release version 0.1.13"
      contains: 'version = "0.1.13"'
    - path: "SKILL.md"
      provides: "Operator contract for default-daemon reuse, serialized local access, exact approval flow, tab cleanup, and marker opt-out"
    - path: ".planning/migration/upstream-sync-2026-04-21.md"
      provides: "Top-level target 10b2086c, 46-commit range metadata, per-commit disposition, migration decisions, and real verification evidence"
  key_links:
    - from: "rust/bins/bhctl/src/main.rs"
      to: "rust/crates/bh-daemon/src/lib.rs"
      via: "ensure-daemon and restart-daemon use the shared generation/lock lifecycle helpers"
      pattern: "(spawn_lock|pending_generation|stop_best_effort)"
    - from: "rust/bins/bhd/src/main.rs"
      to: "rust/crates/bh-daemon/src/lib.rs"
      via: "daemon startup atomically republishes its process generation before the handshake wait"
      pattern: "initialize_runtime_files"
    - from: "rust/crates/bh-daemon/src/lib.rs"
      to: "Target.detachFromTarget"
      via: "js_result cleanup applies evaluation-versus-detach error precedence"
      pattern: "detach_transient_target"
    - from: "rust/bins/bhctl/src/main.rs"
      to: "rust/crates/bh-discovery/src/lib.rs"
      via: "Brave Origin profile path selects the Brave Origin macOS application"
      pattern: "Brave-Origin|Brave Origin"
    - from: ".planning/migration/upstream-sync-2026-04-21.md"
      to: "upstream/main"
      via: "Scope target and daily record use git rev-list baseline..target full-count semantics"
      pattern: "10b2086c29f0696a6712956d2914e03012f5ebd0"
---

<objective>
Migrate the complete upstream Browser Harness range `0c9b95ff6740556dd71d28f9422a953d203358af..10b2086c29f0696a6712956d2914e03012f5ebd0` into the existing Rust daemon, CLI, MCP, and knowledge architecture, then verify, commit, and push the fast-forward-safe result to `origin/main`.

Purpose: Preserve upstream behavioral parity through release 0.1.13 while keeping Python runtime sources upstream-only and maintaining an auditable decision for every commit.
Output: Rust lifecycle/helper/profile changes with focused tests, updated operator guidance and migration audit, one English conventional commit, a GSD quick summary, and synchronized local/origin main refs.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/migration/upstream-sync-2026-04-21.md
@rust/crates/bh-daemon/src/lib.rs
@rust/crates/bh-discovery/src/lib.rs
@rust/bins/bhd/src/main.rs
@rust/bins/bhctl/src/main.rs
@rust/bins/browser-harness-cli/src/main.rs
@SKILL.md
@README.md
@docs/MCP.md
@domains/README.md

<interfaces>
Current Rust lifecycle seam:
- `bh_daemon::{already_running, initialize_runtime_files, log_tail, stop_best_effort, DaemonConfig}` owns runtime files and termination.
- `bhctl::ensure_daemon_output` parses `EnsureDaemonOptions`, launches `bhd`, polls `already_running`, and classifies Chrome setup/approval errors.
- `bhd::main` builds `DaemonConfig`, initializes runtime files, runs `bh_daemon::serve`, and cleans runtime files after safe shutdown.
- `bh_daemon::serve` publishes its Unix socket only after CDP handshake and first-page attachment; local startup currently uses `CdpClient::connect_with_timeout(url, 45)`.

Current helper seam:
- `bh_daemon::js_result(expression, target_id)` creates a transient target session for `target_id`, evaluates through `Runtime.evaluate`, and calls `detach_transient_target`.
- `detach_transient_target` currently discards every detach error; the sync must apply the upstream error-precedence contract at this shared boundary.
- `mark_session` is called from tab switching, set-session, and page-load event handling; this is the single marker gate.

Existing parity evidence:
- `browser-harness-mcp` is already a Cargo workspace member, an installable/expected sibling binary, and documented in `README.md` plus `docs/MCP.md`.
- `browser-harness-cli::resolve_workspace_root` checks the exact supplied or compile-time Cargo workspace path and performs no ancestor repository walk.
- The Rust fork has no recording engine or `events.jsonl` writer; Rust `String` and `serde_json` operate on valid Unicode scalar values.
- The upstream range changes zero files under either domain-skill root; the current transform of both upstream roots, including `scraping.md -> skill.md`, resolves to 111/111 local files.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Port single-flight Chrome approval and Brave Origin discovery through the supported Unix lifecycle</name>
  <precondition>`git status --porcelain=v1` is empty before GSD artifacts, the current branch is `main`, and both configured remote URLs exactly match the repositories named in the user request.</precondition>
  <files>rust/crates/bh-daemon/src/lib.rs, rust/crates/bh-discovery/src/lib.rs, rust/bins/bhd/src/main.rs, rust/bins/bhctl/src/main.rs</files>
  <behavior>
    - Concurrent cold `ensure-daemon` calls for one name serialize on a per-name advisory lock, publish one child generation before releasing it, and join the same pre-socket process.
    - The default local approval wait becomes deadline-free only after `handshake-wait`; remote/CDP startup keeps its 60-second default bound and an explicit `wait` value remains distinguishable from the omitted value.
    - An approval-pending child stays alive with its PID record when the caller yields or reports the approval instruction; a pending child that exits during approval produces a terminal permission error without opening a replacement connection.
    - Pending PID records are atomically written as `{pid,started}` generations; restart cancellation locks the name, rechecks the exact generation and socket readiness, then signals only that generation.
    - Reused PIDs, changed successor generations, and live legacy records without a verifiable start fingerprint keep their ownership files and processes.
    - Linux fingerprints read `/proc/<pid>/stat` field 22 after the final command-name parenthesis; macOS fingerprints use `ps -o lstart= -p <pid>`; unsupported identity probes fail closed.
    - The shared `mark_session` gate reads `BH_TAB_MARKER` with case-insensitive matching: an absent variable and every value outside the disable set keep marking enabled, while `0`, `false`, `no`, and `off` disable marking in any letter case.
    - Tab switching, explicit set-session, and page-load handling continue to route title marking through the shared `mark_session` gate; focused tests cover the enabled default, an explicit enabled value, every disabled spelling, mixed-case variants, and all three marking paths.
    - macOS profile discovery includes `Library/Application Support/BraveSoftware/Brave-Origin`, and launch selection maps it to `open -a "Brave Origin"` ahead of the broader Brave rule.
  </behavior>
  <action>Run the clean-worktree gate before any source edit. Fetch `upstream main` and `origin main` in separate commands, check out `main`, and apply `git merge --ff-only origin/main`; re-run the clean gate after the fast-forward. Read the old target strictly from the top-level `## Scope` block of the migration audit, resolve the fetched target with `git rev-parse upstream/main`, and calculate `git rev-list --count old..target` without merge filtering. When both SHAs match, print `无更新`, keep the worktree clean, and end the workflow before commit or push. For the expected 46-commit range, preserve the fetched SHA as the immutable analysis target.

Implement process-generation parsing, atomic PID publication, per-name advisory locking, exact pending cancellation, and PID-reuse guards in the shared `bh-daemon` lifecycle helpers so `ensure-daemon`, `restart-daemon`, and `bhd` use one authority. Use the existing `libc` dependency and Unix runtime boundary for file locking and process signaling. Keep legacy plain PID reads compatible for readiness observation while granting cancellation authority only to a matching fingerprinted generation. Change `EnsureDaemonOptions.wait_seconds` to preserve omitted-versus-explicit input, recheck socket and pending ownership inside the lock, publish the child PID generation before lock release, and poll with `Instant`. Once a local log enters `handshake-wait`, keep the exact child connection alive and print the same-name `mac-approve` command after two seconds. Use the unbounded `CdpClient::connect` path for local Chrome so the popup-owning handshake persists until approval, process termination, or explicit exact-generation cancellation; retain bounded startup for remote/CDP modes. Add focused unit tests around generation parsing, atomic publication, lock exclusion/release, successor protection, unverifiable ownership preservation, pending-child join/death behavior, deadline selection, and same-name approval instructions.

Implement `BH_TAB_MARKER` at the shared `mark_session` boundary. Parse the final upstream disable set with ASCII case-insensitive equality for `0`, `false`, `no`, and `off`; treat an absent variable and other values as enabled. Return before issuing title-marker CDP work when disabled, and retain the existing `mark_session` calls from tab switching, explicit set-session, and page-load event handling so every title-marking path uses the same decision. Add focused tests for the absent/default case, an explicit enabled value, each lowercase disable spelling, mixed-case word variants, and enabled/disabled behavior reached through all three callers.

Extend `default_browser_profiles` with Brave Origin on macOS and put the specific `brave-origin` launch rule before `brave`; expand existing discovery and `browser_launch_spec_matches_profile_family` coverage. Keep Python runtime files outside the Rust tree.</action>
  <verify>
    <automated>env -u CFLAGS -u CC cargo test --manifest-path rust/Cargo.toml -p bh-daemon tab_marker &amp;&amp; env -u CFLAGS -u CC cargo test --manifest-path rust/Cargo.toml -p bh-daemon -p bh-discovery -p bhctl</automated>
  </verify>
  <done>Focused tests prove one pending connection per daemon name, exact-generation-only cancellation, safe PID-reuse handling, deadline-free default local approval, bounded remote/explicit waits, shared `BH_TAB_MARKER` behavior for enabled defaults plus every final case-insensitive disable spelling across tab-switch/set-session/page-load paths, and Brave Origin discovery plus relaunch selection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align target-scoped detach semantics, install-mode safeguards, MCP packaging evidence, and release version</name>
  <files>rust/crates/bh-daemon/src/lib.rs, rust/bins/browser-harness-cli/src/main.rs, rust/Cargo.toml, rust/Cargo.lock</files>
  <behavior>
    - Target-scoped JavaScript attempts one detach after successful evaluation and after every evaluation failure.
    - Evaluation failure remains the returned error even when cleanup also fails or is interrupted.
    - Successful evaluation ignores only `No session with given id` and `Session with given id not found` detach results; every other detach error reaches the caller.
    - JavaScript without `target_id` creates and detaches no transient session.
    - Workspace and every workspace-local lockfile package report 0.1.13.
    - Exact workspace-root resolution never promotes an enclosing Git/Cargo repository to the install source, and the MCP binary remains present in the workspace install/verify lists.
  </behavior>
  <action>Refine `detach_transient_target` into an error-aware result at the existing shared transient-session boundary. In `js_result`, capture the evaluation result first, always attempt one detach for target-scoped calls, preserve the evaluation error across any cleanup failure, and apply the exact two already-dropped-session message matches only after evaluation succeeds. Let unexpected detach failures replace a successful evaluation result. Keep current best-effort cleanup at other transient callers where their upstream contract stayed unchanged. Add asynchronous regression tests with a fake CDP sender for success, missing-session cleanup, unexpected cleanup failure, evaluation failure plus cleanup failure, and the no-target path.

Raise `[workspace.package].version` and all workspace-local lockfile entries from 0.1.10 to 0.1.13, leaving third-party package versions unchanged. Add a focused installer regression proving `resolve_workspace_root` requires `Cargo.toml` at the exact candidate path even when a parent repository exists, and assert `browser-harness-mcp` remains in the Cargo workspace plus install/verify binary sets. Record the upstream editable-install fix as already guaranteed by this exact-path Rust installer and the MCP entrypoint commits as already satisfied by the installed dependency-free Rust binary. Record the `events.jsonl` lone-surrogate patch as structurally inapplicable because the Rust recording engine and JSONL writer are absent and Rust strings cannot contain lone UTF-16 surrogates. Record Python build pinning as packaging-specific while carrying its release number into the Rust workspace.</action>
  <verify>
    <automated>env -u CFLAGS -u CC cargo test --manifest-path rust/Cargo.toml -p bh-daemon -p browser-harness-cli &amp;&amp; cargo metadata --no-deps --format-version 1 --manifest-path rust/Cargo.toml | rg '"version":"0\.1\.13"' &amp;&amp; rg -n 'browser-harness-mcp' rust/Cargo.toml rust/bins/browser-harness-cli/src/main.rs docs/MCP.md</automated>
  </verify>
  <done>Detach error precedence matches the final upstream behavior, the exact-path installer and packaged MCP entrypoint have regression evidence, all Rust workspace-local packages are 0.1.13, and Python-only changes have explicit applicability decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Update guidance and the 46-commit audit, run the full gate, commit, and push main safely</name>
  <files>SKILL.md, .planning/migration/upstream-sync-2026-04-21.md, .planning/STATE.md, .planning/quick/260905-4a0-sync-upstream-browser-harness-runtime-an/SUMMARY.md</files>
  <action>Adapt the upstream SKILL changes to the typed Rust CLI: narrow the frontmatter description to browser-required work; document `BH_TAB_MARKER=0`; reuse the default local daemon across sites, tasks, and sequential agents; serialize shared local browser operations; reserve distinct remote browsers for simultaneous isolation; preserve the original waiting process and exact `BU_NAME` for one `mac-approve` call; close task-owned tabs when state is disposable; and retain tabs needed for user visibility, follow-up, or unsaved state. Preserve current Rust recording-preview notes. Verify `README.md` and `docs/MCP.md` already express the installed Rust MCP binary and record their upstream changes as already satisfied.

In the audit, update only the top-level Scope metadata to target `10b2086c29f0696a6712956d2914e03012f5ebd0`, range `0c9b95ff6740556dd71d28f9422a953d203358af..10b2086c29f0696a6712956d2914e03012f5ebd0`, and Count `46 commits` using full `git rev-list --count baseline..target` semantics. Append a dated migration section containing a chronological 46-row commit table with full SHA, subject, changed area, semantic disposition, and supersession/merge relationship. Cover the final semantics for single-flight approval, deadline removal, exact pending cancellation, unverifiable ownership, marker opt-out spellings, transient detach precedence, Brave Origin, MCP packaging and missing-extra explanation, skill description/default-daemon serialization/tab hygiene, versions 0.1.11 through 0.1.13, and release pins. State that this range has zero domain-skill changes, while a fresh union scan of `agent-workspace/domain-skills/` and legacy `domain-skills/` maps `scraping.md` to `domains/&lt;site&gt;/skill.md` and reaches 111/111. Include the already-satisfied and structurally inapplicable Rust decisions from Task 2. Keep all Python runtime files upstream-only.

Run the required gates in this order and capture their real key output lines in the audit and GSD summary: `cargo fmt --manifest-path rust/Cargo.toml --all -- --check`; `cargo check --manifest-path rust/Cargo.toml --workspace`; `env -u CFLAGS -u CC cargo test --manifest-path rust/Cargo.toml --workspace`; `env -u CFLAGS -u CC cargo build --manifest-path rust/Cargo.toml --workspace`; `env -u CFLAGS -u CC cargo run --quiet --manifest-path rust/Cargo.toml --bin bhrun -- summary`; `env -u CFLAGS -u CC cargo run --quiet --manifest-path rust/Cargo.toml --bin browser-harness -- --help`; an MCP stdio initialize plus `tools/list` smoke against `browser-harness-mcp`; the 46-row commit-table count; the 111/111 domain transform; `git diff --check`; and `./scripts/scan_sensitive.sh`. On macOS Bash 3.2, run a zsh/rg fallback over `git ls-files -z --cached --others --exclude-standard` with the script's exact 12 PCRE2 patterns and record the fallback, file count, skipped binaries, existing matches, and new-match count. Treat every format, test, smoke, mapping, whitespace, or scan failure as a repair-and-rerun loop. Inspect `git status --short` so every intended new file is staged.

Create one English conventional commit, recommended message `feat: sync upstream browser harness 0.1.13`; when unattended signing alone blocks the commit and repository history confirms unsigned commits, use `git -c commit.gpgsign=false commit`. Fetch `origin main` again immediately before push, require `git merge-base --is-ancestor origin/main HEAD`, then run `git push origin HEAD:main` without force options. Fetch `origin main` after push, require both `git merge-base --is-ancestor HEAD origin/main` and `git rev-list --left-right --count HEAD...origin/main` equal to `0 0`, and require a clean final worktree. Update STATE and SUMMARY with the final commit SHA, upstream target, gate results, push result, and ancestry evidence before the committed/pushed handoff; fold any workflow-authored final metadata into the same clean pushed state.</action>
  <verify>
    <automated>cargo fmt --manifest-path rust/Cargo.toml --all -- --check &amp;&amp; cargo check --manifest-path rust/Cargo.toml --workspace &amp;&amp; env -u CFLAGS -u CC cargo test --manifest-path rust/Cargo.toml --workspace &amp;&amp; env -u CFLAGS -u CC cargo build --manifest-path rust/Cargo.toml --workspace &amp;&amp; env -u CFLAGS -u CC cargo run --quiet --manifest-path rust/Cargo.toml --bin bhrun -- summary &amp;&amp; env -u CFLAGS -u CC cargo run --quiet --manifest-path rust/Cargo.toml --bin browser-harness -- --help &amp;&amp; git diff --check &amp;&amp; git merge-base --is-ancestor HEAD origin/main &amp;&amp; test "$(git rev-list --left-right --count HEAD...origin/main)" = $'0\t0' &amp;&amp; test -z "$(git status --porcelain=v1)"</automated>
  </verify>
  <done>The audit accounts for all 46 commits and real gate outputs, domain coverage is 111/111, the sensitive scan is clean, one conventional commit is on origin/main, local HEAD and origin/main are identical, and the final worktree is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream Git remote → local migration | External commit content supplies behavior and documentation inputs; remote URL, fetched SHA, ancestry, and per-commit disposition anchor trust. |
| bhctl process → runtime PID/lock files | Concurrent local processes coordinate daemon ownership before the Unix socket exists. |
| runtime PID record → OS signal | File contents can become stale or race with process reuse; a signal needs a matching process-start generation under the same lock. |
| MCP stdin → browser-harness CLI | External JSON-RPC requests enter the existing typed CLI delegation boundary. |
| changed files → public Git remote | Documentation and audit text can disclose local paths or credentials unless the repository scan passes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-SYNC-01 | Tampering | upstream/origin refs | high | mitigate | Verify exact remote URLs, fetch separately, pin the immutable target SHA, enumerate all 46 commits, and use ancestry gates around the push. |
| T-SYNC-02 | Spoofing / Elevation of Privilege | PID file and SIGTERM path | high | mitigate | Use atomic `{pid,started}` records, OS process-start fingerprints, per-name advisory lock, generation recheck, and fail-closed handling for unverifiable ownership. |
| T-SYNC-03 | Denial of Service | Chrome approval startup | high | mitigate | Keep one popup-owning handshake, join concurrent callers, remove the default local approval deadline, and suppress automatic replacement after approval failure. |
| T-SYNC-04 | Information Disclosure | docs, audit, logs | medium | mitigate | Keep endpoint redaction intact and run the exact repository sensitive-content patterns across tracked and unignored files before commit. |
| T-SYNC-05 | Tampering | MCP packaging | medium | mitigate | Preserve the existing dependency-free binary, installer/verify lists, and initialize/tools-list smoke; add no package dependency. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Task | Status | Notes
------ | -- | ------------------- | ---- | ------ | -----
GOAL | — | Sync upstream target through Rust architecture, verify, commit, and push | 1-3 | COVERED | End-to-end outcome from quick description
REQ | SYNC-01 | Clean/fast-forward preflight and immutable 46-commit range | 1 | COVERED | Includes zero-update branch
REQ | SYNC-02 | Runtime parity: approval single-flight, marker opt-out, detach, Brave Origin | 1-2 | COVERED | Final superseding upstream semantics drive behavior
REQ | SYNC-03 | Version, MCP/install, docs, domain mapping, per-commit audit | 2-3 | COVERED | Applicable plus explicit applicability decisions
REQ | SYNC-04 | Full gates, conventional commit, ancestry-safe non-force push | 3 | COVERED | Includes post-push equality and clean tree
RESEARCH | — | Pending Chrome approval ownership and cancellation chain (`5ff6e56..0f479f8`) | 1 | COVERED | Final behavior uses generation identity and serialization
RESEARCH | — | Tab marker opt-out chain (`2dfaac5..5ed6110`) | 1 | COVERED | Four case-insensitive false spellings plus enabled default
RESEARCH | — | Target-scoped JS detach chain (`dbbbb2f..835529d`) | 2 | COVERED | Evaluation error has precedence
RESEARCH | — | Brave Origin discovery (`c2f5f3c`) | 1 | COVERED | macOS path and launch app
RESEARCH | — | Editable install, surrogate-safe recording, MCP packaging | 2-3 | COVERED | Already-satisfied or structurally inapplicable decisions recorded with evidence
RESEARCH | — | Skill/default-daemon/shared-access/tab-hygiene docs | 3 | COVERED | Adapted to typed Rust CLI
RESEARCH | — | Releases 0.1.11, 0.1.12, 0.1.13 and Python build pins | 2-3 | COVERED | Rust version follows final upstream release
RESEARCH | — | Domain roots and scraping filename transform | 3 | COVERED | Range delta zero; full union remains 111/111
CONTEXT | — | No quick CONTEXT.md decisions exist | — | COVERED | User request supplies the locked scope
</source_audit>

<verification>
- The working tree is clean before sync edits and after the pushed handoff.
- The top-level audit target moves from `0c9b95ff6740556dd71d28f9422a953d203358af` to `10b2086c29f0696a6712956d2914e03012f5ebd0`; the full range count is 46, with 27 non-merge and 19 merge commits.
- Focused lifecycle/helper/profile tests pass before the full workspace gate.
- All user-specified fmt/check/test/build/CLI/diff/scan commands pass after final edits.
- MCP initialize/tools-list smoke and the 111/111 current-plus-legacy domain mapping pass.
- `origin/main` is an ancestor of the commit before push; local HEAD and fetched `origin/main` are identical after push.
</verification>

<success_criteria>
- One pending local Chrome approval connection exists per daemon name and survives normal caller waiting.
- Explicit cancellation signals only an exact live process generation.
- Marker, detach, Brave Origin, release version, and operator guidance match the final upstream behavior.
- All 46 commits receive an audit disposition, including merges and Python-specific applicability decisions.
- Required verification evidence and true SHAs appear in the migration audit and quick summary.
- The final commit is present on `origin/main` with no force push and no remaining worktree changes.
</success_criteria>

<output>
Create `.planning/quick/260905-4a0-sync-upstream-browser-harness-runtime-an/SUMMARY.md` with final status, upstream SHA transition, commit SHA, verification key lines, push ancestry checks, and clean-worktree evidence.
</output>
