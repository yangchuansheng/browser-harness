# Legacy Domain Skills

`domain-skills/` is the legacy site-knowledge tree.

Use [`../domains/`](../domains/) first for migrated sites. Only fall back to
`domain-skills/` when a site has not moved yet.

Migration rule:

- new site-specific knowledge should go to `domains/<site>/skill.md`
- existing legacy guides here can stay in place until they are migrated
